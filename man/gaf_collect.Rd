% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaf_parquet.R
\name{gaf_collect}
\alias{gaf_collect}
\title{Collect a GAF tbl into an R tibble with proper type coercion}
\usage{
gaf_collect(tbl, filter_not = TRUE, filter_taxon = NULL, evidence_codes = NULL)
}
\arguments{
\item{tbl}{a lazy tbl as returned by \code{\link{gaf_tbl}}, optionally
with dplyr verbs already applied.}

\item{filter_not}{logical.  Exclude NOT annotations.  Default \code{TRUE}.}

\item{filter_taxon}{integer or \code{NULL}.  Filter to primary taxon.}

\item{evidence_codes}{character vector or \code{NULL}.  Filter to
specific evidence codes.}
}
\value{
a tibble equivalent to the output of \code{\link{parse_gaf}}.
}
\description{
Calls \code{dplyr::collect()} on a lazy GAF tbl and applies
post-collection type conversions that cannot be expressed in SQL:
\code{date} is coerced from VARCHAR to \code{Date}, and empty
strings in optional columns are converted to \code{NA}.
}
\details{
Accepts the same filtering arguments as \code{\link{parse_gaf}} so
the result is directly comparable regardless of which backend was used.
}
\examples{
if (has_gaf_parquet("human")) {
  gaf_tbl("human") |>
    dplyr::filter(db_object_symbol == "ORMDL3") |>
    gaf_collect()
}

}
\seealso{
\code{\link{gaf_tbl}}, \code{\link{parse_gaf}}
}
